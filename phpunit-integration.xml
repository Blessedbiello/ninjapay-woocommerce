<?xml version="1.0" encoding="UTF-8"?>
<!--
  Integration test config. Runs against a real WordPress + WooCommerce
  via the WP PHPUnit test library (provisioned by `wp-env`). Distinct
  bootstrap from phpunit.xml, which is unit-only and needs no WP runtime.

  Run with:  wp-env run tests-cli --env-cwd=wp-content/plugins/ninjapay-woocommerce \
               vendor/bin/phpunit -c phpunit-integration.xml
-->
<phpunit
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/9.6/phpunit.xsd"
  bootstrap="tests/integration/bootstrap.php"
  colors="true"
  verbose="true"
  convertErrorsToExceptions="true"
  convertNoticesToExceptions="true"
  convertWarningsToExceptions="true"
>
  <testsuites>
    <testsuite name="integration">
      <directory suffix="Test.php">tests/integration</directory>
    </testsuite>
  </testsuites>
</phpunit>
